# for more info visit https://cto.ai/docs/ops-reference
version: "1"
commands:
  - name: resume-generator:1.0.0
    # Step 1: the Op should be public
    # TODO: update the line below with the correct flag value
    public:
    description: Generate & deploy a resume website on Github Pages.
    # Step 1: the Op should run the built index.js file from the correct path
    # TODO: update the line below with the correct run command; consider the working directory set in the Dockerfile
    run: 
    sdk: "2"
    # Step 1: the Op should be enabled for running in Slack
    # TODO: update the line below with the correct flag value
    remote: 
    sourceCodeURL: "https://github.com/cto-ai/resume-generator"
    src:
      - Dockerfile
      - package.json
      - .dockerignore
      - tsconfig.json
      - src
    mountCwd: false
    mountHome: false
    help:
      usage: This Op requires a Github token. Please review the Op readme for details on how to generate it and set it up as a secret.
      arguments: false
      options: false
